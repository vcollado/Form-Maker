/*
 * app module.
 * @module app
 */

var formMaker = formMaker || {};
formMaker.app = formMaker.app || {};

/*
 * Configura y inicia la aplicacion
 * @class InitA
 * @namespace formMaker.app
 * @constructor
 */
formMaker.app.Launcher = function(appParams) {

  if (appParams) {
    //todo
  }
};
